apiVersion: cluster.open-cluster-management.io/v1beta2
kind: ManagedClusterSetBinding
metadata:
  name: default
  namespace: policies  # El namespace donde se crean tus Placements
spec:
  clusterSet: default  # El set donde viven tus clusters 
